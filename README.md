# three good things

Small experimental app built with Elm.

## Build instructions

Make sure you have Elm installed. Then from the root of the project, run:

```
elm-make Main.elm --output elm.js
```

You can then view the project by opening `index.html` in your browser.
